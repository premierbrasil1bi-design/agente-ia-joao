/**
 * Nome canônico da instância Evolution (evita duplicatas por digitação divergente).
 * Usado como fallback quando o canal não tem external_id nem slug em `instance`.
 */
export const DEFAULT_EVOLUTION_INSTANCE_NAME = 'consultorio_dra_ana_paula';
