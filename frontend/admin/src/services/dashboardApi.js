/**
 * Serviço de API do dashboard – reexporta e centraliza o uso do cliente API.
 * Use createApiClient de api/client com canal e token; esta camada prepara para futuras métricas.
 */

export { createApiClient } from '../api/client';
