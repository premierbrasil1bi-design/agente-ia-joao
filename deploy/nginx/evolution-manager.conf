# OPCIONAL — apenas debug / Evolution Manager (UI oficial).
# O sistema OMNIA NÃO depende deste proxy: app e admin usam apenas /api/evolution/* no backend.
# Se ainda quiser expor a UI da Evolution atrás do Nginx sem configurar apikey no browser, use algo assim:
#
# map $http_upgrade $connection_upgrade {
#   default upgrade;
#   '' close;
# }
#
# server {
#     listen 443 ssl http2;
#     server_name evolution-manager.exemplo.com;
#     location / {
#         proxy_pass http://127.0.0.1:8080;
#         proxy_http_version 1.1;
#         # Mesmo valor que EVOLUTION_API_KEY / AUTHENTICATION_API_KEY no container Evolution (preferir secret no deploy).
#         proxy_set_header apikey "SUBSTITUA_PELA_CHAVE";
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
